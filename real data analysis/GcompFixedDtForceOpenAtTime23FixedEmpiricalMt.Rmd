---
title: "G-computation with Fixed D_t = 1 and Fixed M_t = 0 (All Treatment Regimes)"
output: html_document
---


## Data Generation

```{r, echo=FALSE}
set.seed(1234)
library(sets)
library(coda)
library(haven)
library(plyr) 
library(tidyverse)
library(nlme) 
library(lme4)
library(dplyr)
library(data.table)
library(car)
library(ggplot2)
library(gridExtra)
library(truncnorm)


digCom_data <- read.csv("non-comp_dataset.csv")
digCom_df = data.frame(digCom_data)


#-------------------------------------------------------------------------------------------------------------
#------------------------------------------Data in long format------------------------------------------------
#-------------------------------------------------------------------------------------------------------------
Mode <- function(x) {
  ux <- unique(x)
  ux[which.max(tabulate(match(x, ux)))]
}

M1_t_mode = Mode(digCom_df$daysSinceOpened)

cat("The mode of M1_t is:", M1_t_mode, ".\n")

M2_t_mode = Mode(digCom_df$daysSincePurchased)

cat("The mode of M2_t is:", M2_t_mode, ".\n")


Y_t_mode = Mode(digCom_df$Order_cnt)

cat("The mode of Y_t is:", Y_t_mode, ".\n")


digCom_workdf = data.frame(ID = digCom_df$recipient_id,  
                           GENDER = digCom_df$gender,
                           BASELINE_AGE = digCom_df$age,
                           t = digCom_df$t,
                           Z_t = digCom_df$Discount,
                           D_t = digCom_df$opened, 
                           M1_t = digCom_df$daysSinceOpened,
                           M2_t = digCom_df$daysSincePurchased,
                           Y_t = digCom_df$Order_cnt)


# change GENDER elements from M,F to 1,0, resp.
digCom_workdf <- digCom_workdf %>%
  mutate(GENDER = ifelse(GENDER == "M", 1, ifelse(GENDER == "F", 0, NA)))

head(digCom_workdf)


M1_t_mode = Mode(digCom_workdf$M1_t)

cat("The mode of M1_t is:", M1_t_mode, ".\n")

M2_t_mode = Mode(digCom_workdf$M2_t)

cat("The mode of M2_t is:", M2_t_mode, ".\n")


Y_t_mode = Mode(digCom_workdf$Y_t)

cat("The mode of Y_t is:", Y_t_mode, ".\n")


M1t_plot = ggplot(digCom_workdf, aes(x  = M1_t)) +
        geom_density() +
        labs(title = "M1_t") +
        theme_minimal()


M2t_plot = ggplot(digCom_workdf, aes(x  = M2_t)) +
        geom_density() +
        labs(title = "M2_t") +
        theme_minimal()


Yt_plot = ggplot(digCom_workdf, aes(x  = Y_t)) +
        geom_density() +
        labs(title = "Y_t") +
        theme_minimal()


grid.arrange(M1t_plot, M2t_plot,Yt_plot,  nrow = 1)


#-------------------------------------------------------------------------------------------------------------
#------------------------------------------Data in wide format------------------------------------------------
#-------------------------------------------------------------------------------------------------------------
setDT(digCom_workdf)
digCom_workdf_wide = dcast(digCom_workdf, ID + GENDER+ BASELINE_AGE   ~ t, 
                         value.var = c("Z_t", "D_t", "M1_t", "M2_t","Y_t"))
head(digCom_workdf_wide)


```


### ALWAYS treated sub population

```{r}
digCom_workdf_wide_alwaysTrt = digCom_workdf_wide[Z_t_1==1 & Z_t_2==1 & Z_t_3==1, ]
head(digCom_workdf_wide_alwaysTrt)
cat("The number of people who were always assigned treatment in the dataset is: ", 
    nrow(digCom_workdf_wide_alwaysTrt), ".\n")

# Var(Y|Z_t_1 = 1, Z_t_2 = 1, Z_t_3 = 1)
var_Y_always_trt = var(digCom_workdf_wide_alwaysTrt$Y_t_3)
var_Y_always_trt


model_always_trt = lm(Y_t_3 ~ D_t_1 + D_t_2 + D_t_3 + BASELINE_AGE + GENDER, 
                      data = digCom_workdf_wide_alwaysTrt)
R1_always_trt = summary(model_always_trt)$r.squared
R1_always_trt

#sample sensitivity parameter k1 (total variance not explained by d_t and L_0)
k1 = runif(1000,0,1)


bias_bound_always_trt = sqrt(var_Y_always_trt * (1- R1_always_trt) * k1)

head(bias_bound_always_trt, 10)


# Plot 

library(ggplot2)
plot_sensitivity_always_trt_df = data.frame(Index = 1:length(bias_bound_always_trt),
                                            Value = bias_bound_always_trt)
plot_sen_always_trt = ggplot(plot_sensitivity_always_trt_df, aes(x = Index, y = Value)) +
                      geom_point(color = "blue", size = 1) +  # Plot the points
                      geom_line(color = "red", size = 0.4) +  # Draw a line through the points
                      labs(title = "Line Plot of Data Points", x = "Index", y = "Value") +  # Add labels
                      theme_minimal()  # Use a clean theme

plot_sen_always_trt
```


### NEVER treated sub population

```{r}
digCom_workdf_wide_neverTrt = digCom_workdf_wide[Z_t_1==0 & Z_t_2==0 & Z_t_3==0, ]
head(digCom_workdf_wide_neverTrt)
cat("The number of people who were never assigned treatment in the dataset is: ", 
    nrow(digCom_workdf_wide_neverTrt), ".\n")


# Var(Y|Z_t_1 = 0, Z_t_2 = 0, Z_t_3 = 0)
var_Y_never_trt = var(digCom_workdf_wide_neverTrt$Y_t_3)
var_Y_never_trt 


model_never_trt <- lm(Y_t_3 ~ D_t_1 + D_t_2 + D_t_3 + BASELINE_AGE + GENDER, 
                      data = digCom_workdf_wide_neverTrt)
R1_never_trt <- summary(model_never_trt)$r.squared
R1_never_trt


#sample sensitivity parameter k0 (total variance not explained by d_t and L_0)
k0 = runif(1000,0,1)


bias_bound_never_trt = sqrt(var_Y_never_trt * (1- R1_never_trt) * k0)

head(bias_bound_never_trt, 10)


# Plot 

plot_sensitivity_never_trt_df = data.frame(Index = 1:length(bias_bound_never_trt),
                                            Value = bias_bound_never_trt)
plot_sen_never_trt = ggplot(plot_sensitivity_never_trt_df, aes(x = Index, y = Value)) +
                      geom_point(color = "blue", size = 1) +  # Plot the points
                      geom_line(color = "red", size = 0.4) +  # Draw a line through the points
                      labs(title = "Line Plot of Data Points", x = "Index", y = "Value") +  # Add labels
                      theme_minimal()  # Use a clean theme

plot_sen_never_trt
```


```{r}

#-------------------------------------------------------------------------------------------------------------
#------------------------------------------Input data---------------------------------------------------------
#-------------------------------------------------------------------------------------------------------------


#-------------------------------------------------------------------------------------------------------------
#------------------------------------------Recent at entry sub-population------------------------------------------------
#-------------------------------------------------------------------------------------------------------------


#Remove rows with a missing column

 digCom_workdf_wide = na.omit(digCom_workdf_wide)


#L0 df order columns by intercept, binary covariates, continuous covariates
L0_intercept = rep(1,length(digCom_workdf_wide$ID))
L0_df_wide = data.frame(Intercept = L0_intercept,
                   GENDER = digCom_workdf_wide$GENDER, BASELINE_AGE = digCom_workdf_wide$BASELINE_AGE) 

head(L0_df_wide)


Y_vec_wide = digCom_workdf_wide$Y_t_3
head(Y_vec_wide)


```


## Load the Nimble parameter samples as a dataframe

```{r}

num_iters = 1000; num_chains = 4
n_rows = num_iters/num_chains

# save parameter samples as a list of 4 matrices: chain1, chain2, chain3, chain4
parameter_samples_edp_long_saved <- readRDS("param_post1000_N10M4_AllData_samples.RDS")


# Names of the chains in the list
chain_names <- names(parameter_samples_edp_long_saved)


# Do this if 4 chains
posterior_samples_long = do.call(rbind, lapply(parameter_samples_edp_long_saved, as.data.frame))


# Do this if 1 chain


```


## Functions to implement G-computation


```{r, echo=FALSE}

# returns matrix of dimensions num_MC_samples by 2
compute_EDPM_cluster_component_number = function(
                         BNPModels_df,
                         num_MC_samples,# number of MC samples to be generated
                         N,
                         M,
                         it   # an integer from 1:Q (number of post iterations)
                         ){
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  outer_comp_vec = sample(x = 1:N, size = num_MC_samples,
                           replace = TRUE, prob = ksi_r_vec)
   
   # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(outer_comp_vec, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_mat_iter = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]})  
   # vector of length num_MC_samples  
    inner_comp_vec = apply(ksi_sr_mat_iter, 2, 
                           function(ksi_sr){sample(x = c(1:M),size =1, 
                                                         replace = TRUE,
                                                         prob = ksi_sr)})

    return_df = data.frame(outer_cluster = outer_comp_vec, inner_cluster = inner_comp_vec)
    return(return_df)
}


sample_L0_bin_single = function(MCdensity= NULL,
                         BNPModels_df,
                         EDPM_cluster_component_number_df,   # df of dim num_MC_samples times 2
                         num_MC_samples,                     # number of MC samples to be generated
                         N,
                         M,
                         it                                  # an integer from 1:Q (number of post iterations)
                         ){
  
  
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  

   pr_Bin_L0_colnames_ordered = unlist(
      lapply(1:N, function(i) paste0("pr_Bin_L0i[", i, ", ", 1:M, "]"))
   )
  
   pr_Bin_L0_colnames = pr_Bin_L0_colnames_ordered[pr_Bin_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   pr_Bin_L0_vec_all_clusters = as.numeric(sapply(pr_Bin_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
   
   
  # Treat the nested finite mixture model as a single-level mixture model with N*M components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] * as.numeric(ksi_sr_mat[,i])))
  }
  
  
  if (is.null(MCdensity)) {
  # Extract coefficients for the specific iteration: vector of length num_MCsamples
  param_coeff_pr_Bin_L0_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){BNPModels_df[[paste0("pr_Bin_L0i[", x[1], ", ", x[2], "]")]][it]}))
  }else{
    ### Sample component numbers (one for each MC sample) based on the total number of clusters (N*M)
    ## Compute weights/new mixture probabilities
    # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  #cat("The number of 0s in  weights_num  vector is:", sum(weights_num == 0) , ".\n")
  weights_num = as.data.frame(weights_num)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
    component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
    })
    #print(component_vec)
    param_coeff_pr_Bin_L0_vec = pr_Bin_L0_vec_all_clusters[component_vec]
    #print(param_coeff_pr_Bin_L0_vec)
  }
  
  
  # Sample the binary outcome from a Bernoulli distribution
  random_samples =  rbinom(num_MC_samples, 1, param_coeff_pr_Bin_L0_vec)
   
   
 #density_dataframe: matrix if dim  num_MC_samples by (N*M) 
  density_dataframe = as.data.frame(sapply(pr_Bin_L0_vec_all_clusters, function(p) {
                                   dbinom(random_samples, size = 1, prob = p)
                       }))
  
 
     return_list = list(
       samples =random_samples,
       density_df = density_dataframe
     )
  
  #return_df = data.frame(samples =random_samples, density =  density_values)
  
  return(return_list)

}


sample_L0_bin_multiple = function(MCdensity,
                         BNPModels_df,
                         EDPM_cluster_component_number_df,   # df of dim num_MC_samples times 2
                         num_MC_samples,                     # number of MC samples to be generated
                         N,
                         M,
                         L0_bin_number,
                         it                                  # an integer from 1:Q (number of post iterations)
                         ){
  
  
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  
   
   pr_Bin_L0_colnames_ordered = unlist(
      lapply(1:N, function(i) paste0("pr_Bin_L0i[", i, ", ", 1:M,", ", L0_bin_number, "]"))
   )
   
   
   pr_Bin_L0_colnames = pr_Bin_L0_colnames_ordered[pr_Bin_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   pr_Bin_L0_vec_all_clusters = as.numeric(sapply(pr_Bin_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
   
   
  # Treat the nested finite mixture model as a single-level mixture model with N*M components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] * as.numeric(ksi_sr_mat[,i])))
  }
  
  
  if (L0_bin_number == 1) {
  # Extract coefficients for the specific iteration: vector of length num_MCsamples
  param_coeff_pr_Bin_L0_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){
        BNPModels_df[[paste0("pr_Bin_L0i[", x[1], ", ", x[2],", ", 1, "]")]][it]}))
  }else{
    ### Sample component numbers (one for each MC sample) based on the total number of clusters (N*M)
    ## Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_num = as.data.frame(weights_num)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
    
    
    component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
    })
    #print(component_vec)
    param_coeff_pr_Bin_L0_vec = pr_Bin_L0_vec_all_clusters[component_vec]
    #print(param_coeff_pr_Bin_L0_vec)
  }
  
  
  # Sample the binary outcome from a Bernoulli distribution
  random_samples =  rbinom(num_MC_samples, 1, param_coeff_pr_Bin_L0_vec)
   
   
 #density_dataframe: matrix if dim  num_MC_samples by (N*M) 
  density_dataframe = as.data.frame(sapply(pr_Bin_L0_vec_all_clusters, function(p) {
                                   dbinom(random_samples, size = 1, prob = p)
                       }))
  
 
     return_list = list(
       samples =random_samples,
       density_df = density_dataframe
     )
  
  #return_df = data.frame(samples =random_samples, density =  density_values)
  
  return(return_list)

}


sample_L0_cont_single = function(MCdensity= NULL,
                         BNPModels_df,
                         EDPM_cluster_component_number_df,   # df of dim num_MC_samples times 2
                         num_MC_samples,                     # number of MC samples to be generated
                         N,
                         M,
                         number_of_L0_bin,
                         it                                  # an integer from 1:Q (number of post iterations)
){
  
  
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  

   mu_Cont_L0_colnames_ordered = unlist(
      lapply(1:N, function(i) paste0("mu_Cont_L0i[", i, ", ", 1:M, "]"))
   )
  
   mu_Cont_L0_colnames = mu_Cont_L0_colnames_ordered[mu_Cont_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   mu_Cont_L0_vec_all_clusters = as.numeric(sapply(mu_Cont_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
 
   sigma_sq_Cont_L0_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("sigma_sq_Cont_L0i[", i, ", ", 1:M, "]"))
   )
  
   sigma_sq_Cont_L0_colnames =  sigma_sq_Cont_L0_colnames_ordered[
                                    sigma_sq_Cont_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   sigma_sq_Cont_L0_vec_all_clusters = as.numeric(sapply(sigma_sq_Cont_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
   
  # treat the nested finite mixture model as a single-level mixture model with N*M  components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] *
                                                      as.numeric(ksi_sr_mat[,i])))
  }
  
  
  if (number_of_L0_bin ==0) {
  # Extract coefficients for the specific iteration: vectors of length num_MCsamples
  param_coeff_mean_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){BNPModels_df[[paste0("mu_Cont_L0i[", x[1], ", ", x[2], "]")]][it]}))
  
  param_coeff_var_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){BNPModels_df[[paste0("sigma_sq_Cont_L0i[", x[1], ", ", x[2], "]")]][it]}))
  
  }else{
    ### Sample component numbers (one for each MC sample) based on the total number of clusters (N*M)
    ## Compute weights/new mixture probabilities
    # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
    # Handle rows where weights_denom was zero
      sample(c(1:(N*M)), size = 1, prob = probabilities)
    })
    param_coeff_mean_vec = mu_Cont_L0_vec_all_clusters[component_vec]
    param_coeff_var_vec = sigma_sq_Cont_L0_vec_all_clusters[component_vec]
  }
  
  
  # Sample the continuous outcome from a Normal distribution
  random_samples = rtruncnorm(n = num_MC_samples, a = 0, b = Inf, 
                              mean = param_coeff_mean_vec, sd = sqrt(param_coeff_var_vec))
  
  
  density_dataframe=  as.data.frame(sapply(1:(N*M), function(i) {
                              dtruncnorm(x = random_samples,a = 0, b = Inf,
                                    mean = mu_Cont_L0_vec_all_clusters[i], 
                                    sd = sqrt(sigma_sq_Cont_L0_vec_all_clusters[i]))
                       }))
  
  
  return_list = list(
    samples =random_samples,
    density_df = density_dataframe 
  )
  return(return_list)
  
}


sample_L0_cont_multiple = function(MCdensity= NULL,
                         BNPModels_df,
                         EDPM_cluster_component_number_df,   # df of dim num_MC_samples times 2
                         num_MC_samples,                     # number of MC samples to be generated
                         N,
                         M,
                         L0_cont_number,
                         number_of_L0_bin,
                         it                                  # an integer from 1:Q (number of post iterations)
){
  
  
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  

   mu_Cont_L0_colnames_ordered = unlist(
      lapply(1:N, function(i) paste0("mu_Cont_L0i[", i, ", ", 1:M,", ", L0_cont_number, "]"))
   )
  
   mu_Cont_L0_colnames = mu_Cont_L0_colnames_ordered[mu_Cont_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   mu_Cont_L0_vec_all_clusters = as.numeric(sapply(mu_Cont_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
 
   sigma_sq_Cont_L0_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("sigma_sq_Cont_L0i[", i, ", ", 1:M,", ", L0_cont_number, "]"))
   )
  
   sigma_sq_Cont_L0_colnames =  sigma_sq_Cont_L0_colnames_ordered[
                                    sigma_sq_Cont_L0_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #vector of size N*M
   sigma_sq_Cont_L0_vec_all_clusters = as.numeric(sapply(sigma_sq_Cont_L0_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
   
  # treat the nested finite mixture model as a single-level mixture model with N*M  components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] *
                                                      as.numeric(ksi_sr_mat[,i])))
  }
  
  
  if (number_of_L0_bin ==0) {
  # Extract coefficients for the specific iteration: vectors of length num_MCsamples
  param_coeff_mean_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){
        BNPModels_df[[paste0("mu_Cont_L0i[", x[1], ", ", x[2],", ", L0_cont_number, "]")]][it]}))
  
  param_coeff_var_vec = as.numeric(apply(EDPM_cluster_component_number_df,1,
      function(x){BNPModels_df[[paste0("sigma_sq_Cont_L0i[", x[1], ", ", x[2],", ", L0_cont_number, "]")]][it]}))
  
  }else{
    ### Sample component numbers (one for each MC sample) based on the total number of clusters (N*M)
    ## Compute weights/new mixture probabilities
    
    # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
    
    # Handle rows where weights_denom was zero
    #weights[zero_rows, ] = 1 / ncol(weights)  # Assign equal weights_dfacross columns
    component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
    })
    param_coeff_mean_vec = mu_Cont_L0_vec_all_clusters[component_vec]
    param_coeff_var_vec = sigma_sq_Cont_L0_vec_all_clusters[component_vec]
  }
  
  
  # Sample the continuous outcome from a Normal distribution
  random_samples = rtruncnorm(n = num_MC_samples, a = 0, b = Inf, 
                              mean = param_coeff_mean_vec, sd = sqrt(param_coeff_var_vec))
  
  
  density_dataframe=  as.data.frame(sapply(1:(N*M), function(i) {
                              dtruncnorm(x = random_samples,a = 0, b = Inf,
                                    mean = mu_Cont_L0_vec_all_clusters[i], 
                                    sd = sqrt(sigma_sq_Cont_L0_vec_all_clusters[i]))
                       }))
  
  
  return_list = list(
    samples =random_samples,
    density_df = density_dataframe 
  )
  return(return_list)
  
}


get_Zt_density_df = function(x,                                  #vector at which we want to compute the density 
                             MCdata,
                             BNPModels_df,
                             num_MC_samples,                     # number of MC samples to be generated
                             N,
                             M,
                             num_L0,                             # number of L0 INCLUDING intercept
                             t,                                  # t = {1, ..., T}
                             it                                  # an integer from 1:Q (number of post iterations)
){
  
  #extract coefficients for the specific iteration ( param_coeff_theta_Zt_df is a num_components * num_L0 dim. matrix)
  param_coeff_theta_Zt_df = matrix(nrow = N*M, ncol = num_L0)
  for(k in 1:num_L0){
    theta_Zt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("theta_Z_t[", i, ", ", 1:M,", ", k,", ", t, "]"))
   )
  
   theta_Zt_colnames =  theta_Zt_colnames_ordered[theta_Zt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #df with nrow = N.M, ncol = num_L0
   param_coeff_theta_Zt_df[,k] = as.numeric(sapply(theta_Zt_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  }
  
  
  # sample random intercept for all MCsamples
  param_coeff_bi_Zt_sig_sq = BNPModels_df$bi_Z_sig_sq[it]
  bi_Zt = rnorm(num_MC_samples, mean = 0, sd = sqrt(param_coeff_bi_Zt_sig_sq))
  
  linpred_df = (as.matrix(MCdata[,1:num_L0]) %*% t(as.matrix(param_coeff_theta_Zt_df))) + bi_Zt # this is num_MCsample * MN dim df
  
  density_df_prob = pnorm(linpred_df)                #do this for probit regression
  
  
  density_dataframe = as.data.frame(apply(density_df_prob,2, function(p) {
    dbinom(x, size = 1, prob = p)              # compute num_MCsample many density values at each col of density_df_prob
  }))
  
  
  return(density_dataframe)
  
  
}


sample_Dt = function(MCdata,                             # MC data upto this point in the temporal order
                     D1_vec,                             # vector of fixed D1 values
                     MCdensity,                          # MC density upto this point in the temporal order
                     BNPModels_df,
                     num_MC_samples,                     # number of MC samples to be generated
                     N,
                     M,
                     num_D_covs,                         # number of D covariates (time-varying)
                     t,                                  # t = {1, ..., T}
                     it                                  # an integer from 1:Q (number of post iterations)
){
  
  #extract coefficients for the specific iteration ( param_coeff_theta_Dt_df is a num_components * num_L0 dim. matrix)
  param_coeff_theta_Dt_df = matrix(nrow = N*M, ncol = num_D_covs)
  for(k in 1:num_D_covs){
    theta_Dt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("theta_D_t", t, "[", i, ", ", 1:M,", ", k, "]"))
   )
  
   #df with nrow = N.M, ncol = num_L0
   param_coeff_theta_Dt_df[,k] = as.numeric(sapply(theta_Dt_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  }
  
  
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster, sum(col) =1
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  
  
  # sample random intercept for all MCsamples
  param_coeff_bi_Dt_sig_sq = BNPModels_df$bi_D_sig_sq[it]
  bi_Dt = rnorm(num_MC_samples, mean = 0, sd = sqrt(param_coeff_bi_Dt_sig_sq))
  
  #print(bi_Dt)
  # treat the nested finite mixture model as a single-level mixture model with N*M components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] *
                                                      as.numeric(ksi_sr_mat[,i])))
  }
  
  
  # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_num = as.data.frame(weights_num)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
  
  
  component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
  })
  
  
  # Extract sampling probability params vector (size num_D_covs) based on the component number drawn by using the component probabilities
  theta_Dt_sample_df = param_coeff_theta_Dt_df[component_vec,]  # this is a df with nrows = num_MC_samples, ncol = num_D_covs at iter it and time t
  # Compute sampling probability
  
  
  #message("reached Dt before sample generation \n")
  # do this for probit regression
  theta_Dt_prob = pnorm(rowSums(MCdata[,1:num_D_covs] *theta_Dt_sample_df) + bi_Dt)
  
  
  # Sample the binary outcome from a Bernoulli distribution
  #random_samples =  rbinom(num_MC_samples, 1, theta_Dt_prob)
  # Fix everyone's Dt =1
  random_samples =  rep(1,num_MC_samples)
  
  
  linpred_df = (as.matrix(MCdata[,1:num_D_covs]) %*% t(as.matrix(param_coeff_theta_Dt_df))) + bi_Dt # this is num_MCsample * MN dim df
  
  
  density_df_prob = pnorm(linpred_df)  # do this for probit regression
  
  if(t==1){
    density_dataframe = as.data.frame(apply(density_df_prob,2, function(p) {
      dbinom(D1_vec, size = 1, prob = p)              # compute num_MCsample many density values at each col of density_df_prob
    }))
    return_list = list(
      density_df = density_dataframe
    )
    return(return_list)
    
  }else{
    density_dataframe = as.data.frame(apply(density_df_prob,2, function(p) {
      dbinom(random_samples, size = 1, prob = p)              # compute num_MCsample many density values at each col of density_df_prob
    }))
    
    return_list = list(
      samples =random_samples,
      density_df = density_dataframe
    )
    return(return_list)
  }
  
  
}


#-----------------------------------------------------------------------------------------
#-----------------------------------------------------------------------------------------


rnorm_hurdle = function(n,mean,sd, hurdle_prob){
  hurdle_comp = rbinom(length(hurdle_prob),1,hurdle_prob)
  return_sample = (1-hurdle_comp)* rtruncnorm(n = length(hurdle_comp), a = 1, b=Inf, mean =mean, sd = sd )
  return(as.numeric(return_sample))
}

dnorm_hurdle = function(x,mean,sd, hurdle_prob){
  density = ifelse(x ==0, hurdle_prob,
                   ((1-hurdle_prob)*dtruncnorm(x = x,a = 1, b=Inf, mean =mean, sd = sd)))
  return(as.numeric(density))
}

#-----------------------------------------------------------------------------------------
#-----------------------------------------------------------------------------------------


sample_hurdle_data = function(MCdata,     # MC data upto this point in the temporal order
                     MCdensity,           # MC density upto this point in the temporal order
                     BNPModels_df,
                     Mediator_num,        # mediator location in temporal order 1,...,J
                     num_MC_samples,      # number of MC samples to be generated
                     N,
                     M,
                     num_M_covs,          # number of M covariates (time-varying)
                     t,                   # t = {1, ..., T}
                     it                   # an integer from 1:Q (number of post iterations)
){

  
  #extract coefficients for the specific iteration ( param_coeff_mean_df is a num_components * num_L0 dim. matrix)
  param_coeff_mean_df = matrix(nrow = N*M, ncol = num_M_covs)
  for(k in 1:num_M_covs){
    theta_Mt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("theta_M",Mediator_num,"_t", t, "[", 
                                   i, ", ", 1:M,", ", k, "]"))
   )
  
   theta_Mt_colnames =  theta_Mt_colnames_ordered[theta_Mt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #df with nrow = N.M, ncol = num_L0
   param_coeff_mean_df[,k] = as.numeric(sapply(theta_Mt_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  }
  
  
   sigSq_Mt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("sigma_sq_M",Mediator_num,"_t[", 
                                   i, ", ", 1:M,", ", t, "]"))
   )
  
   sigSq_Mt_colnames =  sigSq_Mt_colnames_ordered[sigSq_Mt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   # vector of  length NM
   param_coeff_var_vec = as.numeric(sapply(sigSq_Mt_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
  
   pi0_Mt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("pi0_M",Mediator_num,"_t[", 
                                   i, ", ", 1:M,", ", t, "]"))
   )
  
   pi0_Mt_colnames =  pi0_Mt_colnames_ordered[pi0_Mt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   # vector of  length NM
   param_coeff_hurdle_prob_vec = as.numeric(sapply(pi0_Mt_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
     
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster, sum(col) =1
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]}) 
  
  
  # sample random intercept for all MCsamples 
  bi_Mt_col_name = paste0("bi_M",Mediator_num,"_sig_sq")
  param_coeff_bi_Mt_sig_sq = BNPModels_df[[bi_Mt_col_name]][it]
  bi_vec = rnorm(num_MC_samples, mean = 0, sd = sqrt(param_coeff_bi_Mt_sig_sq))
  
  
  # treat the nested finite mixture model as a single-level mixture model with N*M components and compute component probs
  total_component_prob = c()  #vector of size N*M
  for(i in 1:N){
    total_component_prob = c(total_component_prob, (ksi_r_vec[i] *
                                                      as.numeric(ksi_sr_mat[,i])))
  }
  
  
  # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_num = as.data.frame(weights_num)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom
  
  
  component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
  })
  
  
  # Extract sampling mean params vector (size num_M_covs) based on the component number drawn by using the component probabilities
  mean_sample_df = param_coeff_mean_df[component_vec,]  # this is a df with nrows = num_MC_samples, ncol = num_M_covs at iter it and time t
  # Compute sampling mean
  mean_sample_vec = rowSums(MCdata[,1:num_M_covs] *mean_sample_df) + bi_vec
  
  # Extract sampling var param based on the component number drawn by using the component probabilities
  var_sample_vec = param_coeff_var_vec[component_vec]
  
  # Extract sampling hurdle prob param based on the component number drawn by using the component probabilities
  
  hurdle_prob_sample_vec = param_coeff_hurdle_prob_vec[component_vec]
  
  
  random_samples =  rnorm_hurdle(num_MC_samples, mean_sample_vec , sqrt(var_sample_vec),hurdle_prob_sample_vec)
  
  
  density_df_mean = as.matrix(MCdata[,1:num_M_covs]) %*% t(as.matrix(param_coeff_mean_df)) + bi_vec # this is num_MCsample * MN dim df
  
  
  density_dataframe=  as.data.frame(sapply(1:(N*M), function(i) {
    dnorm_hurdle(random_samples, mean = density_df_mean[,i], 
                 sd = sqrt(param_coeff_var_vec[i]),
                 hurdle_prob =param_coeff_hurdle_prob_vec[i])
  }))
  
  return_list = list(
    samples =random_samples,
    density_df = density_dataframe
  )
  
  return(return_list)
}


sample_hurdle_data_fix_mediator = function(MCdata,     # MC data upto this point in the temporal order
                     MCdensity,           # MC density upto this point in the temporal order
                     BNPModels_df,
                     Mediator_num,        # mediator location in temporal order 1,...,J
                     num_MC_samples,      # number of MC samples to be generated
                     N,
                     M,
                     num_M_covs,          # number of M covariates INCLUDING intercept (varies by t)
                     t,                   # t = {1, ..., T}
                     it,                   # an integer from 1:Q (number of post iterations)
                     empirical_mediator_vec
){


  #extract coefficients for the specific iteration ( param_coeff_mean_df is a num_components * num_M_covs dim. matrix)
  param_coeff_mean_df = matrix(nrow = N*M, ncol = num_M_covs)
  for(k in 1:num_M_covs){
    theta_Mt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("theta_M",Mediator_num,"_t", t, "[",
                                   i, ", ", 1:M,", ", k, "]"))
   )

   theta_Mt_colnames =  theta_Mt_colnames_ordered[theta_Mt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #df with nrow = N.M, ncol = num_M_covs
   param_coeff_mean_df[,k] = as.numeric(sapply(theta_Mt_colnames,
                                        function(col_name) BNPModels_df[[col_name]][it]))
  }


   sigSq_Mt_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("sigma_sq_M",Mediator_num,"_t[",
   )

   pi0_Mt_colnames =  pi0_Mt_colnames_ordered[pi0_Mt_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   # vector of  length NM
   param_coeff_hurdle_prob_vec = as.numeric(sapply(pi0_Mt_colnames,
                                        function(col_name) BNPModels_df[[col_name]][it]))


  #get the col num of all cols whose name begin with ksi_r
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N

  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})

   # matrix of dimension M by N: a column for each outer cluster, sum(col) =1
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]})
  }

   # Compute weights/new mixture probabilities
  weights_num = sweep(MCdensity, 2, total_component_prob, `*`)
  weights_num = as.data.frame(weights_num)
  weights_denom=  rowSums(weights_num)

  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }

  weights_df = weights_num / weights_denom


  component_vec =  apply(weights_df, 1, function(probabilities) {
      sample(c(1:(N*M)), size = 1, prob = probabilities)
  })


  #--------------------------------------------------------------


  # Extract sampling mean params vector (size num_M_covs) based on the component number drawn by using the component probabilities
  mean_sample_df = param_coeff_mean_df[component_vec,]  # this is a df with nrows = num_MC_samples, ncol = num_M_covs at iter it and time t
  # Compute sampling mean
  mean_sample_vec = rowSums(MCdata[,1:num_M_covs] *mean_sample_df) + bi_vec

  # Extract sampling var param based on the component number drawn by using the component probabilities
  var_sample_vec = param_coeff_var_vec[component_vec]

  # Extract sampling hurdle prob param based on the component number drawn by using the component probabilities

  hurdle_prob_sample_vec = param_coeff_hurdle_prob_vec[component_vec]


  random_samples =  sample(empirical_mediator_vec, num_MC_samples, replace = TRUE)


  density_df_mean = as.matrix(MCdata[,1:num_M_covs]) %*% t(as.matrix(param_coeff_mean_df)) + bi_vec # this is num_MCsample * MN dim df

  density_dataframe=  as.data.frame(sapply(1:(N*M), function(i) {
    dnorm_hurdle(random_samples, mean = density_df_mean[,i],
                 sd = sqrt(param_coeff_var_vec[i]),
                 hurdle_prob =param_coeff_hurdle_prob_vec[i])
  }))

  return_list = list(
    samples =random_samples,
    density_df = density_dataframe
  )

  return(return_list)
}


compute_Y_mean = function(MCdata,                             # MC data upto this point in the temporal order
                              MCdensity,                      # MC density upto this point in the temporal order
                              BNPModels_df,
                              num_MC_samples,                     # number of MC samples to be generated
                              N,
                              M,
                              num_covariates,               # number of local regression INCLUDING intercept
                              it                                  # an integer from 1:Q (number of post iterations)
){
  
    #extract coefficients for the specific iteration ( param_coeff_mean_df is a num_components * num_covariates dim. matrix)
  beta_Y_colnames_ordered = unlist(lapply(1:N, function(i) paste0("beta_Yi[", 
                                   i, ", ", 1:num_covariates, "]")))
  beta_Y_colnames =  beta_Y_colnames_ordered[beta_Y_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   #df with nrow = N, ncol = num_covariates
  param_coeff_mean_df = matrix(sapply(beta_Y_colnames, 
                            function(col_name) BNPModels_df[[col_name]][it]),
                            nrow = N, ncol = num_covariates, byrow = TRUE)
  
  
   sigSq_Y_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("sigma_sq_Yi[", 
                                   i, "]"))
   )
  
   sigSq_Y_colnames =  sigSq_Y_colnames_ordered[sigSq_Y_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   # vector of  length N
   param_coeff_var_vec = as.numeric(sapply(sigSq_Y_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
  
   pi0_Y_colnames_ordered = unlist(
    lapply(1:N, function(i) paste0("pi0_Y[", i,  "]"))
   )
  
   pi0_Y_colnames =  pi0_Y_colnames_ordered[pi0_Y_colnames_ordered %in%
                                                   colnames(BNPModels_df)]
   # vector of  length N
   param_coeff_hurdle_prob_vec = as.numeric(sapply(pi0_Y_colnames, 
                                        function(col_name) BNPModels_df[[col_name]][it]))
  
     
  #get the col num of all cols whose name begin with ksi_r                       
  ksi_r_col_num = grep("^ksi_r\\[\\d+\\]$", colnames(BNPModels_df))
  #get the elements of all cols whose name begin with ksi_r for this iter
  ksi_r_vec = as.numeric(BNPModels_df[it, ksi_r_col_num]) # outer comp prob vec of length N
  
  # matrix of dimension M by num_MC_samples: a column for each MC sample
   ksi_sr_col_name = sapply(1:N, function(outer_comp) {
   paste0("ksi_sr[", outer_comp, ", ", 1:M, "]")  # Column names for ksi_sr[outer_comp,1] to ksi_sr[outer_comp,M]
})
  
   # matrix of dimension M by N: a column for each outer cluster, sum(col) =1
   ksi_sr_mat = apply(ksi_sr_col_name, c(1,2),
                            function(col_name){BNPModels_df[[col_name]][it]})
  
   ksi_sr_df = as.data.frame(t(ksi_sr_mat)) # nrow = N, ncol = M, sum(row) =1
  
  
  # Compute weights/new mixture probabilities
  weights_num = as.data.frame(matrix(nrow = num_MC_samples, ncol = N))
  for (i in 1:N) {
    start_col = (i - 1) * M + 1           #{1, M + 1, 2M + 1, 3M + 1, ... }
    end_col = i * M                       #{M, 2M, 3M, 4M, ...}
    #MCdensity_times_ksi_sr[,start_col:end_col] = MCdensity[, start_col:end_col] * ksi_sr_df[i,]
    # Sum the rows for each block of M columns
    weights_num[, i] = rowSums(MCdensity[, start_col:end_col]* 
                                 as.numeric(ksi_sr_df[i,])) * 
                      ksi_r_vec[i]
  }
  
  
  weights_denom=  rowSums(weights_num)
  
  zero_rows <- is.na(weights_denom) | (weights_denom == 0)

  if (any(zero_rows)) {
    ncomp <- ncol(weights_num)                    # N*M
    # set each problematic row to 1 for all comps (then will normalize below)
    weights_num[zero_rows, ] <- matrix(1, nrow = sum(zero_rows), ncol = ncomp, byrow = TRUE)
    # recompute denom
    weights_denom <- rowSums(weights_num)
  }
  weights_df = weights_num/weights_denom
  # Handle rows where weights_denom was zero
  #weights[zero_rows, ] = 1 / ncol(weights)  # Assign equal weights_dfacross columns
  
  
  #sample the outer component number for each num_MC_samples subjects
  outer_comp_num = apply(weights_df, 1, function(w) sample(1:ncol(weights_df), size = 1, prob = w))
  # vectors of size num_MC_samples
  param_coeff_hurdle_prob_MC_samples = param_coeff_hurdle_prob_vec[outer_comp_num]
  # binary variable with 0 indicating Y =0 and 1 indicating Y~Normal()
  # param_coeff_hurdle_location_MC_samples = rbinom(num_MC_samples, size = 1, 
  #                                                 prob = 1 - param_coeff_hurdle_prob_MC_samples)
  # vector of size num_MC_samples
  #param_coeff_var_MC_samples = param_coeff_var_vec[outer_comp_num]
  
  
  # this is num_MCsample * N dim df of untruncated normal mean
  density_df_mean =as.matrix(MCdata[,1:num_covariates]) %*% t(as.matrix(param_coeff_mean_df)) 
  #density_df_mean = density_df_mean^2
  
  
  # compute mean for truncated Normal
  z = sweep(-density_df_mean, 2, sqrt(param_coeff_var_vec), `/`)
  phi_z = dnorm(z)  # PDF of the standard normal at z
  Phi_z = pnorm(z)  # CDF of the standard normal at z
  trunc_norm_param_denom = pmax(1 - Phi_z, 1e-10)
  density_df_mean_trunc_norm = density_df_mean + sweep(phi_z / trunc_norm_param_denom, 2, 
                                                        sqrt(param_coeff_var_vec), FUN = "*")
  
  
  # E[Y|...] = sum_j(w_j * (1-pi0_j) * E[trunc_norm_j])
  density_df_mean_Y_hurdle = sweep(density_df_mean_trunc_norm, 2, 1-param_coeff_hurdle_prob_vec, FUN = "*")
  mean_Y = rowSums(density_df_mean_Y_hurdle * weights_df)

  return(mean_Y)
}


####################################################################
######.............Function for MC integration .............########
MCInteg <- function(var.type, # Vector of variable specifications for data.  Fi=fixed (e.g. the exposure), D=trt received, M1= mediator 1,  M2= mediator 2, Y=outcome. 
                    BNPModels, #fitted BNP model parameter samples list
                    fixed.regime.trt, # A vector specifying the Exposure  regime Z
                    fixed.regime.control, # A vector specifying the Exposure  regime Z_star
                    U1,       # could be one of: "c" for VALUE ATTENTIVE, "d" for PRICE ATTENTIVE, "n" for NON-ACTIVE CUSTOMERS and "a" for ACTIVE CUSTOMERS
                    J=10000, # Size of pseudo data. Default is set to 2,000.
                    Ndraws=1000, # Number of posterior draws. Default is set to 200.
                    num_outer_clusters,
                    num_inner_clusters,
                    num_Bin_L0,
                    num_Cont_L0,
                    fixed_mediator = FALSE,
                    empirical_M1_list = NULL,
                    empirical_M2_list = NULL,
                    ...
){

  if (fixed_mediator == TRUE && (is.null(empirical_M1_list) || is.null(empirical_M2_list))) stop("You have chosen empirical mediator sampling but have not provided empirical_M1_list or empirical_M2_list")

  n_Y <- length(which(var.type=="Y")) # Number of outcome variables, should be equal to 1
  n_L0 <- length(which(var.type=="L0")) # Number of baseline confounder variables INCLUDING the intercept, >1 allowed
  n_Fi <- length(which(var.type=="Fi")) # Number of fixed variables, >1 allowed
  if(!is.null(fixed.regime.trt)) {
    n_Reg_trt <- max(ifelse(!is.null(fixed.regime.trt), length(fixed.regime.trt), 0))
  } else {
    n_Reg_trt <- 1
  }
  
  if(!is.null(fixed.regime.control)) {
    n_Reg_control <- max(ifelse(!is.null(fixed.regime.control), length(fixed.regime.control), 0))
  } else {
    n_Reg_control <- 1
  }
  
  if (n_Y != 1) stop("The number of outcome variables is not equal to 1 ")
  #if (ncol(data) != length(var.type)) stop("The number of columns in data is not equal to the length of var.type ")
  
  if(!is.null(fixed.regime.trt)){
    if (length(fixed.regime.trt) != n_Fi) stop("Warning: The number of fixed variables is not equal to the length of the fixed regime(s)")
  }
  
  if(!is.null(fixed.regime.control)){
    if (length(fixed.regime.control) != n_Fi) stop("Warning: The number of fixed variables is not equal to the length of the fixed regime(s)")
  }
  
  if (!(U1 %in% c("c", "d", "n", "a"))) {
    stop("U1 must be one of 'c', 'd', 'n', or 'a'")
  }
  
  
  ########################################################
  ## Create matrices to store the output. 
  
  
  Y_zzstar_mat <- matrix(nrow =  1 , ncol = Ndraws)
  MC_error_vec = c()
  
  
  for(it in 1:Ndraws) {
    zt <- 1                               # zt indexes FIXED longitudinal trt regime in G-comp
    dt <- 1
    mt <- 1
    # Per-time-point covariate dimensions for D and M
    dt_positions = which(var.type == "Dt")
    m2t_positions = which(var.type == "M2t")
    K_D_vec = sapply(dt_positions, function(pos) n_L0 + sum(var.type[1:pos] == "Fi"))
    K_M_vec = sapply(m2t_positions, function(pos) n_L0 + sum(var.type[1:pos] == "Fi") + sum(var.type[1:pos] == "Dt"))
    # Track Z and D history for building time-specific design matrices
    Z_history_trt = NULL; Z_history_control = NULL
    D_history_trt = NULL; D_history_control = NULL
    intercept = rep(1, J)
    # initialize a matrix to store density values used for computing cluster component probs
    density_prod_df = matrix(1, nrow = J, ncol = num_outer_clusters* num_inner_clusters)
    # sample data for the first binary baseline confounder (Add a loop for L0 data if the number of L0 data increase)
    EDPM_cluster_component_number = compute_EDPM_cluster_component_number(
                         BNPModels_df = BNPModels,
                         num_MC_samples = J,
                         N = num_outer_clusters,
                         M = num_inner_clusters,
                         it = it   
                         )   
    
    if(num_Bin_L0 ==1){
      L0_bin_sampled_vec_iter = sample_L0_bin_single(MCdensity= NULL,
                         BNPModels_df= BNPModels,
                         EDPM_cluster_component_number_df = EDPM_cluster_component_number,
                         num_MC_samples = J,
                         N = num_outer_clusters,
                         M = num_inner_clusters,
                         it = it)
      L0_data = cbind(intercept, L0_bin_sampled_vec_iter$samples)
      density_prod_df = density_prod_df * L0_bin_sampled_vec_iter$density_df
      
    } else if(num_Bin_L0 >1){
      L0_bin_sampled_mat_iter = NULL # initialize a matrix to store samples
      for (k in 1: num_Bin_L0){
        L0_bin_sampled_vec_iter_k = sample_L0_bin_multiple(
                         MCdensity= density_prod_df,
                         BNPModels_df= BNPModels,
                         EDPM_cluster_component_number_df = EDPM_cluster_component_number,
                         num_MC_samples = J,
                         N = num_outer_clusters,
                         M = num_inner_clusters,
                         L0_bin_number = k,
                         it = it)
        L0_bin_sampled_mat_iter = cbind(L0_bin_sampled_mat_iter,
                                        L0_bin_sampled_vec_iter_k$samples)
        density_prod_df = density_prod_df * L0_bin_sampled_vec_iter_k$density_df
      }
      
      L0_data = cbind(intercept, L0_bin_sampled_mat_iter)
    }
    
   
    # sample data for the continuous baseline confounder
    if(num_Cont_L0 ==1){
      L0_cont_sampled_vec_iter = sample_L0_cont_single(MCdensity= density_prod_df,
                         BNPModels_df= BNPModels,
                         EDPM_cluster_component_number_df = EDPM_cluster_component_number,
                         num_MC_samples = J,
                         N = num_outer_clusters,
                         M = num_inner_clusters,
                         number_of_L0_bin = num_Bin_L0,
                         it = it)
    L0_data = cbind(L0_data, L0_cont_sampled_vec_iter$samples)
    density_prod_df = density_prod_df * L0_cont_sampled_vec_iter$density_df
      
    } else if(num_Cont_L0 >1){
      L0_cont_sampled_mat_iter = NULL # initialize a matrix to store samples
      for (k in 1: num_Cont_L0){
        L0_cont_sampled_vec_iter_k = sample_L0_cont_multiple(
                         MCdensity= density_prod_df,
                         BNPModels_df= BNPModels,
                         EDPM_cluster_component_number_df = EDPM_cluster_component_number,
                         num_MC_samples = J,
                         N = num_outer_clusters,
                         M = num_inner_clusters,
                         L0_cont_number = k,
                         number_of_L0_bin = num_Bin_L0,
                         it = it)
        L0_cont_sampled_mat_iter = cbind(L0_cont_sampled_mat_iter,
                                        L0_cont_sampled_vec_iter_k$samples)
        density_prod_df = density_prod_df * L0_cont_sampled_vec_iter_k$density_df
      }
      
      L0_data = cbind(L0_data, L0_cont_sampled_mat_iter)
    }
    
    
   # design matrix for Y regression (initialize with L0_data)
   Y_design_mat = L0_data
   # Zt_df = as.data.frame(matrix(rep(fixed.regime.trt, each = J), nrow = J, ncol = length(fixed.regime.trt)))  
   # Y_design_mat = cbind(L0_data, Zt_df)   
    
    
    Z1_trt = rep(fixed.regime.trt[1], J)
    Z1_control = rep(fixed.regime.control[1], J)
    if (U1 == "c") {
      D1_trt = Z1_trt
      D1_control = Z1_control
    } else if (U1 == "d") {
      D1_trt = 1- Z1_trt
      D1_control = 1-Z1_control
    } else if (U1 == "n") {
      D1_trt = rep(0,J)
      D1_control = rep(0,J)
    } else if (U1 == "a") {
      D1_trt = rep(1,J)
      D1_control = rep(1,J)
    }
    
    
    for (j in (n_L0+1):length(var.type)) {                                         
      
    if(var.type[j] == "Fi") {
      Zt_density_df_trt = get_Zt_density_df(x = rep(fixed.regime.trt[zt], J),            
                                            MCdata = L0_data,
                                            BNPModels_df = BNPModels,
                                            num_MC_samples = J,  
                                            N = num_outer_clusters,
                                            M = num_inner_clusters,
                                            num_L0= n_L0,                             
                                            t =zt,                                    
                                            it= it )
         
        
       Zt_density_df_control = get_Zt_density_df(x = rep(fixed.regime.control[zt], J),    
                                                MCdata =L0_data,
                                                BNPModels_df= BNPModels,
                                                num_MC_samples = J, 
                                                N = num_outer_clusters,
                                                M = num_inner_clusters,
                                                num_L0= n_L0, 
                                                t = zt,                                  
                                                it = it)
         
        density_prod_df_trt = density_prod_df * Zt_density_df_trt
        density_prod_df_control = density_prod_df * Zt_density_df_control
        Y_design_mat = cbind(Y_design_mat, rep(fixed.regime.trt[zt], J))
        Z_history_trt = cbind(Z_history_trt, rep(fixed.regime.trt[zt], J))
        Z_history_control = cbind(Z_history_control, rep(fixed.regime.control[zt], J))
        zt = zt + 1
      }
      else if(var.type[j] == "Dt") {
         # Build D design matrix: L0 + Z history up to current time
         D_MCdata_trt = cbind(L0_data, Z_history_trt[, 1:dt, drop=FALSE])
         D_MCdata_control = cbind(L0_data, Z_history_control[, 1:dt, drop=FALSE])
         Dt_sampled_vec_iter_trt = sample_Dt(MCdata = D_MCdata_trt,
                                             D1_vec = D1_trt,                             
                                             MCdensity = density_prod_df_trt,
                                             BNPModels_df= BNPModels,
                                              num_MC_samples = J,
                                              N=num_outer_clusters,
                                              M=num_inner_clusters,
                                              num_D_covs = K_D_vec[dt],
                                              t = dt,
                                              it =it)
        
        
         Dt_sampled_vec_iter_control = sample_Dt(MCdata = D_MCdata_control,
                                                D1_vec = D1_control,                      
                                                MCdensity= density_prod_df_control,
                                                BNPModels_df= BNPModels,
                                                num_MC_samples = J,
                                                N=num_outer_clusters,
                                                M=num_inner_clusters,
                                                num_D_covs = K_D_vec[dt],
                                                t = dt,
                                                it =it)
        
        
        density_prod_df_trt = density_prod_df_trt * Dt_sampled_vec_iter_trt$density_df
        density_prod_df_control = density_prod_df_control * Dt_sampled_vec_iter_control$density_df
        if(dt == 1){
          Y_design_mat = cbind(Y_design_mat, D1_trt)
          D_history_trt = cbind(D_history_trt, D1_trt)
          D_history_control = cbind(D_history_control, D1_control)
        }else{
          Y_design_mat = cbind(Y_design_mat, Dt_sampled_vec_iter_trt$samples)
          D_history_trt = cbind(D_history_trt, Dt_sampled_vec_iter_trt$samples)
          D_history_control = cbind(D_history_control, Dt_sampled_vec_iter_control$samples)
        }
        dt = dt +1
      }
      else if(var.type[j] == "M1t") {
        # Build M covariate data: L0 + interleaved (Z1,D1,Z2,D2,...) up to time mt
        M_history_control = NULL
        for(tt in 1:mt){
          M_history_control = cbind(M_history_control,
                                    Z_history_control[, tt, drop=FALSE],
                                    D_history_control[, tt, drop=FALSE])
        }
        M_MCdata_control = cbind(L0_data, M_history_control)

        if(fixed_mediator == TRUE){
          M1t_sampled_vec_iter_control = sample_hurdle_data_fix_mediator(MCdata= M_MCdata_control,
                                          MCdensity = density_prod_df_control,
                                          BNPModels_df= BNPModels,
                                          Mediator_num = 1,
                                          num_MC_samples = J,
                                          N=num_outer_clusters,
                                          M=num_inner_clusters,
                                          num_M_covs = K_M_vec[mt],
                                          t = mt,
                                          it =it,
                                          empirical_mediator_vec = empirical_M1_list[[mt]])

          density_prod_df_trt = density_prod_df_trt * M1t_sampled_vec_iter_control$density_df    # Mt density arises from the control regime
        density_prod_df_control = density_prod_df_control * M1t_sampled_vec_iter_control$density_df
          Y_design_mat = cbind(Y_design_mat, M1t_sampled_vec_iter_control$samples)

        }else{
          M1t_sampled_vec_iter_control = sample_hurdle_data(MCdata= M_MCdata_control,
                                            MCdensity = density_prod_df_control,
                                            BNPModels_df= BNPModels,
                                            Mediator_num = 1,
                                            num_MC_samples = J,
                                            N=num_outer_clusters,
                                            M=num_inner_clusters,
                                            num_M_covs = K_M_vec[mt],
                                            t = mt,
                                            it =it)


        density_prod_df_trt = density_prod_df_trt * M1t_sampled_vec_iter_control$density_df    # Mt density arises from the control regime
        density_prod_df_control = density_prod_df_control * M1t_sampled_vec_iter_control$density_df
        Y_design_mat = cbind(Y_design_mat, M1t_sampled_vec_iter_control$samples)
        }

      }
      else if(var.type[j] == "M2t") {
        # Build M covariate data: L0 + interleaved (Z1,D1,Z2,D2,...) up to time mt
        M_history_control_M2 = NULL
        for(tt in 1:mt){
          M_history_control_M2 = cbind(M_history_control_M2,
                                       Z_history_control[, tt, drop=FALSE],
                                       D_history_control[, tt, drop=FALSE])
        }
        M_MCdata_control_M2 = cbind(L0_data, M_history_control_M2)

        if(fixed_mediator == TRUE){
          M2t_sampled_vec_iter_control = sample_hurdle_data_fix_mediator(MCdata= M_MCdata_control_M2,
                                          MCdensity = density_prod_df_control,
                                          BNPModels_df= BNPModels,
                                          Mediator_num = 2,
                                          num_MC_samples = J,
                                          N=num_outer_clusters,
                                          M=num_inner_clusters,
                                          num_M_covs = K_M_vec[mt],
                                          t = mt,
                                          it =it,
                                          empirical_mediator_vec = empirical_M2_list[[mt]])

          density_prod_df_trt = density_prod_df_trt * M2t_sampled_vec_iter_control$density_df    # Mt density arises from the control regime
        density_prod_df_control = density_prod_df_control * M2t_sampled_vec_iter_control$density_df
          Y_design_mat = cbind(Y_design_mat, M2t_sampled_vec_iter_control$samples)

        }else{
          M2t_sampled_vec_iter_control = sample_hurdle_data(MCdata= M_MCdata_control_M2,
                                            MCdensity = density_prod_df_control,
                                            BNPModels_df= BNPModels,
                                            Mediator_num = 2,
                                            num_MC_samples = J,
                                            N=num_outer_clusters,
                                            M=num_inner_clusters,
                                            num_M_covs = K_M_vec[mt],
                                            t = mt,
                                            it =it)


        density_prod_df_trt = density_prod_df_trt * M2t_sampled_vec_iter_control$density_df    # Mt density arises from the control regime
        density_prod_df_control = density_prod_df_control * M2t_sampled_vec_iter_control$density_df
        Y_design_mat = cbind(Y_design_mat, M2t_sampled_vec_iter_control$samples)
        }
        mt = mt + 1
      }
      else if(var.type[j] == "Y") {
        Y_mean_vec = compute_Y_mean(MCdata= Y_design_mat, 
                                MCdensity= density_prod_df_trt, 
                                BNPModels_df= BNPModels,
                                num_MC_samples= J,
                                N =num_outer_clusters,
                                M=num_inner_clusters,
                                num_covariates= ncol(Y_design_mat),
                                it = it )
        
        Y_zzstar_mat[1,it] = mean(Y_mean_vec)
        MC_error_vec[it] = sd(Y_mean_vec) / sqrt(length(Y_mean_vec))
        if(it == 500){
          mc_error = sd(Y_mean_vec) / sqrt(length(Y_mean_vec))
          cat("The MC Error at iteration ", it, "is: ", mc_error, ".\n")
          cat("MC estimate at iteration ", it, "is: ", Y_zzstar_mat[1,it], ".\n")
        }
      }
    }
  }
  MC_error_to_post_SD = MC_error_vec/ sd(as.numeric(Y_zzstar_mat[1,]))
  plot(as.numeric(MC_error_to_post_SD), main = "MC Error relative to Posterior SD by iter")
  return(Y_zzstar_mat)
}


compute_PIDE = function(theta_10, theta_00){
  return(theta_10 - theta_00)
}

compute_PJIIE = function(theta_11, theta_10){
  return(theta_11 - theta_10)
}


compute_PCE = function(PIDE, PJIIE){
  return(PIDE+PJIIE)
}

compute_total_effects = function(theta_11, theta_00){
  return(theta_11 - theta_00)
}


# calculate_95bayesian_credible_interval <- function(data) {
#   cred_int = function(data_row){
#     posterior_samples <- data_row
#     posterior_means <- as.numeric(format(round(mean(posterior_samples) , 4), nsmall = 4))
#     quantiles <- quantile(posterior_samples, c(0.025, 0.975))
#     interval <- as.numeric(format(round(quantiles, 4), nsmall = 4))
#     return(interval)
#   }
#   
#   return_mat = apply(data,1,cred_int)
#   return(t(return_mat))
#   
# }

calculate_95bayesian_credible_interval  = function(data_row){
    posterior_samples = as.numeric(data_row)
    posterior_mean =  as.numeric(format(round(mean(posterior_samples) , 4), nsmall = 4))
    posterior_median =  as.numeric(format(round(median(posterior_samples) , 4), nsmall = 4))
    quantiles = quantile(posterior_samples, c(0.025, 0.975))
    interval = as.numeric(format(round(quantiles, 4), nsmall = 4))
    return_df = data.frame(posterior_mean = posterior_mean, posterior_median = posterior_median,
                           lower_95CI = interval[1],   upper_95CI = interval[2])
    return(return_df)
  }
  

library(tidyr)

plot_ConfInt <-function(conf_int_mat) { 
  conf_int_df = data.frame(conf_int_mat)
  conf_int_df$CI <- 1:nrow(conf_int_df)
  ci_long <- gather(conf_int_df, key = "Bound", value = "Value", -CI)
  ggplot(ci_long, aes(x = CI, ymin = Value, ymax = Value, color = Bound )) +
    geom_errorbar(width = 0.2)+
    labs(x = "Visits", y = "Confidence/Credible Intervals")+
    scale_color_manual(values = c("lower" = "red", "upper" = "blue")) +
    theme_minimal()
  
}


```


## Function calls


## Active Customers




## Function calls

```{r}

input_varType1 = c("L0","L0","L0","Fi", "Dt", "M1t","M2t", "Fi", "Dt", "M1t","M2t","Fi", "Dt", "M1t","M2t", "Y")

```



## G-computation: Regime Z111 (Zstar000)


### Active Customers

```{r}

empirical_M1 = list(
  digCom_workdf_wide$M1_t_1,
  digCom_workdf_wide$M1_t_2,
  digCom_workdf_wide$M1_t_3
)
empirical_M2 = list(
  digCom_workdf_wide$M2_t_1,
  digCom_workdf_wide$M2_t_2,
  digCom_workdf_wide$M2_t_3
)


active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z001 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,1),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,1),
                                        fixed.regime.control = c(0,0,1),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,1),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,1),
                                        fixed.regime.control = c(0,0,1),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,1),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,1),
                                        fixed.regime.control = c(0,0,1),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,0,1),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,1),
                                        fixed.regime.control = c(0,0,1),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z010 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(0,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(0,1,0),
                                fixed.regime.control = c(0,1,0),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(0,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(0,1,0),
                                fixed.regime.control = c(0,1,0),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(0,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(0,1,0),
                                fixed.regime.control = c(0,1,0),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(0,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(0,1,0),
                                fixed.regime.control = c(0,1,0),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z011 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,1,1),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,1,1),
                                        fixed.regime.control = c(0,1,1),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,1,1),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,1,1),
                                        fixed.regime.control = c(0,1,1),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,1,1),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,1,1),
                                        fixed.regime.control = c(0,1,1),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,1),
                                    fixed.regime.control = c(0,1,1),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,1),
                                fixed.regime.control = c(1,1,1),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,1,1),
                                        fixed.regime.control = c(0,1,1),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z100 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,0),
                                fixed.regime.control = c(1,0,0),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,0),
                                fixed.regime.control = c(1,0,0),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,0),
                                fixed.regime.control = c(1,0,0),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,0),
                                fixed.regime.control = c(1,0,0),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z101 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,1),
                                fixed.regime.control = c(1,0,1),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,1),
                                fixed.regime.control = c(1,0,1),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,1),
                                fixed.regime.control = c(1,0,1),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,0,1),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,0,1),
                                fixed.regime.control = c(1,0,1),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```




## G-computation: Regime Z110 (Zstar000)


### Active Customers

```{r}

active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "a",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zzstar))

active_customer_theta_zzstar_sen = active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zzstar_sen))


active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,0),
                                fixed.regime.control = c(1,1,0),
                                U1 = "a",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zz))

active_customer_theta_zz_sen = active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(active_customer_theta_zz_sen))


active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "a",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(active_customer_theta_zstarzstar))

active_customer_theta_zstarzstar_sen = active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(active_customer_theta_zstarzstar_sen))

```


## Active Customers class

### Direct Effects PIDE

```{r}
active_customer_PIDE = compute_PIDE(active_customer_theta_zzstar, active_customer_theta_zstarzstar)
head(as.data.frame(active_customer_PIDE[,1:10]))
active_customer_PIDE_CI = calculate_95bayesian_credible_interval(active_customer_PIDE)
active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PIDE_sen = compute_PIDE(active_customer_theta_zzstar_sen, active_customer_theta_zstarzstar_sen)
active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PIDE_sen)
active_customer_PIDE_sen_CI

active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PIDE_sen, active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
active_customer_PJIIE = compute_PJIIE(active_customer_theta_zz, active_customer_theta_zzstar)
head(as.data.frame(active_customer_PJIIE[,1:10]))
active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE)
active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PJIIE_sen = compute_PJIIE(active_customer_theta_zz_sen, active_customer_theta_zzstar_sen)
active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PJIIE_sen)
active_customer_PJIIE_sen_CI

active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PJIIE_sen, active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
active_customer_PCE = compute_PCE(active_customer_PIDE,active_customer_PJIIE)
head(as.data.frame(active_customer_PCE[,1:10]))
active_customer_PCE_CI = calculate_95bayesian_credible_interval(active_customer_PCE)
active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
active_customer_PCE_sen = compute_total_effects(active_customer_theta_zz_sen, active_customer_theta_zstarzstar_sen)
active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(active_customer_PCE_sen)
active_customer_PCE_sen_CI

active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(active_customer_PCE_sen, active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Value Attentive

```{r}

value_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "c",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zzstar))

value_attentive_theta_zzstar_sen = value_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zzstar_sen))


value_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,0),
                                fixed.regime.control = c(1,1,0),
                                U1 = "c",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zz))

value_attentive_theta_zz_sen = value_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(value_attentive_theta_zz_sen))


value_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "c",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(value_attentive_theta_zstarzstar))

value_attentive_theta_zstarzstar_sen = value_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(value_attentive_theta_zstarzstar_sen))

```


## Value Attentive class

### Direct Effects PIDE

```{r}
value_attentive_PIDE = compute_PIDE(value_attentive_theta_zzstar, value_attentive_theta_zstarzstar)
head(as.data.frame(value_attentive_PIDE[,1:10]))
value_attentive_PIDE_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE)
value_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(value_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PIDE_sen = compute_PIDE(value_attentive_theta_zzstar_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PIDE_sen)
value_attentive_PIDE_sen_CI

value_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PIDE_sen, value_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
value_attentive_PJIIE = compute_PJIIE(value_attentive_theta_zz, value_attentive_theta_zzstar)
head(as.data.frame(value_attentive_PJIIE[,1:10]))
value_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE)
value_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(value_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PJIIE_sen = compute_PJIIE(value_attentive_theta_zz_sen, value_attentive_theta_zzstar_sen)
value_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PJIIE_sen)
value_attentive_PJIIE_sen_CI

value_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PJIIE_sen, value_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
value_attentive_PCE = compute_PCE(value_attentive_PIDE,value_attentive_PJIIE)
head(as.data.frame(value_attentive_PCE[,1:10]))
value_attentive_PCE_CI = calculate_95bayesian_credible_interval(value_attentive_PCE)
value_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(value_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
value_attentive_PCE_sen = compute_total_effects(value_attentive_theta_zz_sen, value_attentive_theta_zstarzstar_sen)
value_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(value_attentive_PCE_sen)
value_attentive_PCE_sen_CI

value_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(value_attentive_PCE_sen, value_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(value_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Price Attentive

```{r}

price_attentive_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "d",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zzstar))

price_attentive_theta_zzstar_sen = price_attentive_theta_zzstar - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zzstar_sen))


price_attentive_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,0),
                                fixed.regime.control = c(1,1,0),
                                U1 = "d",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zz))

price_attentive_theta_zz_sen = price_attentive_theta_zz - bias_bound_always_trt
plot(as.numeric(price_attentive_theta_zz_sen))


price_attentive_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "d",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(price_attentive_theta_zstarzstar))

price_attentive_theta_zstarzstar_sen = price_attentive_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(price_attentive_theta_zstarzstar_sen))

```


## Price Attentive class

### Direct Effects PIDE

```{r}
price_attentive_PIDE = compute_PIDE(price_attentive_theta_zzstar, price_attentive_theta_zstarzstar)
head(as.data.frame(price_attentive_PIDE[,1:10]))
price_attentive_PIDE_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE)
price_attentive_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(price_attentive_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PIDE_sen = compute_PIDE(price_attentive_theta_zzstar_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PIDE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PIDE_sen)
price_attentive_PIDE_sen_CI

price_attentive_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PIDE_sen, price_attentive_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
price_attentive_PJIIE = compute_PJIIE(price_attentive_theta_zz, price_attentive_theta_zzstar)
head(as.data.frame(price_attentive_PJIIE[,1:10]))
price_attentive_PJIIE_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE)
price_attentive_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(price_attentive_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PJIIE_sen = compute_PJIIE(price_attentive_theta_zz_sen, price_attentive_theta_zzstar_sen)
price_attentive_PJIIE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PJIIE_sen)
price_attentive_PJIIE_sen_CI

price_attentive_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PJIIE_sen, price_attentive_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
price_attentive_PCE = compute_PCE(price_attentive_PIDE,price_attentive_PJIIE)
head(as.data.frame(price_attentive_PCE[,1:10]))
price_attentive_PCE_CI = calculate_95bayesian_credible_interval(price_attentive_PCE)
price_attentive_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(price_attentive_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
price_attentive_PCE_sen = compute_total_effects(price_attentive_theta_zz_sen, price_attentive_theta_zstarzstar_sen)
price_attentive_PCE_sen_CI = calculate_95bayesian_credible_interval(price_attentive_PCE_sen)
price_attentive_PCE_sen_CI

price_attentive_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(price_attentive_PCE_sen, price_attentive_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(price_attentive_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```



### Non-Active Customers

```{r}

non_active_customer_theta_zzstar = MCInteg(var.type = input_varType1,
                                    BNPModels = posterior_samples_long,
                                    fixed.regime.trt = c(1,1,0),
                                    fixed.regime.control = c(0,0,0),
                                    U1 = "n",
                                    J=10000,
                                    Ndraws= 1000,
                                    num_outer_clusters =10,
                                    num_inner_clusters=4,
                                    num_Bin_L0 = 2,
                                    num_Cont_L0 = 0,
                                    fixed_mediator = TRUE,
                                    empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zzstar))

non_active_customer_theta_zzstar_sen = non_active_customer_theta_zzstar - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zzstar_sen))


non_active_customer_theta_zz = MCInteg(var.type = input_varType1,
                                BNPModels = posterior_samples_long,
                                fixed.regime.trt = c(1,1,0),
                                fixed.regime.control = c(1,1,0),
                                U1 = "n",
                                J=10000,
                                Ndraws= 1000,
                                num_outer_clusters =10,
                                num_inner_clusters=4,
                                num_Bin_L0 = 2,
                                num_Cont_L0 = 0,
                                fixed_mediator = TRUE,
                                empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zz))

non_active_customer_theta_zz_sen = non_active_customer_theta_zz - bias_bound_always_trt
plot(as.numeric(non_active_customer_theta_zz_sen))


non_active_customer_theta_zstarzstar = MCInteg(var.type = input_varType1,
                                        BNPModels = posterior_samples_long,
                                        fixed.regime.trt = c(0,0,0),
                                        fixed.regime.control = c(0,0,0),
                                        U1 = "n",
                                        J=10000,
                                        Ndraws= 1000,
                                        num_outer_clusters =10,
                                        num_inner_clusters=4,
                                        num_Bin_L0 = 2,
                                        num_Cont_L0 = 0,
                                        fixed_mediator = TRUE,
                                        empirical_M1_list = empirical_M1,
                                    empirical_M2_list = empirical_M2)
plot(as.numeric(non_active_customer_theta_zstarzstar))

non_active_customer_theta_zstarzstar_sen = non_active_customer_theta_zstarzstar - bias_bound_never_trt
plot(as.numeric(non_active_customer_theta_zstarzstar_sen))

```


## Non-Active Customers class

### Direct Effects PIDE

```{r}
non_active_customer_PIDE = compute_PIDE(non_active_customer_theta_zzstar, non_active_customer_theta_zstarzstar)
head(as.data.frame(non_active_customer_PIDE[,1:10]))
non_active_customer_PIDE_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE)
non_active_customer_PIDE_CI
cat("The posterior proportion of PIDE < 0 is: ",mean(non_active_customer_PIDE<0), "\n" )
```


### Direct Effects PIDE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PIDE_sen = compute_PIDE(non_active_customer_theta_zzstar_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PIDE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PIDE_sen)
non_active_customer_PIDE_sen_CI

non_active_customer_PIDE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PIDE_sen, non_active_customer_PIDE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PIDE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Indirect Effects PJIIE

```{r}
non_active_customer_PJIIE = compute_PJIIE(non_active_customer_theta_zz, non_active_customer_theta_zzstar)
head(as.data.frame(non_active_customer_PJIIE[,1:10]))
non_active_customer_PJIIE_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE)
non_active_customer_PJIIE_CI
cat("The posterior proportion of PJIIE < 0 is: ",mean(non_active_customer_PJIIE<0), "\n" )
```


### Indirect Effects PJIIE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PJIIE_sen = compute_PJIIE(non_active_customer_theta_zz_sen, non_active_customer_theta_zzstar_sen)
non_active_customer_PJIIE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PJIIE_sen)
non_active_customer_PJIIE_sen_CI

non_active_customer_PJIIE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PJIIE_sen, non_active_customer_PJIIE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PJIIE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


### Total Effects PCE

```{r}
non_active_customer_PCE = compute_PCE(non_active_customer_PIDE,non_active_customer_PJIIE)
head(as.data.frame(non_active_customer_PCE[,1:10]))
non_active_customer_PCE_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE)
non_active_customer_PCE_CI
cat("The posterior proportion of PCE < 0 is: ",mean(non_active_customer_PCE<0), "\n" )
```


### Total Effects PCE with sensitivity analysis for Assumption 3.3

```{r}
non_active_customer_PCE_sen = compute_total_effects(non_active_customer_theta_zz_sen, non_active_customer_theta_zstarzstar_sen)
non_active_customer_PCE_sen_CI = calculate_95bayesian_credible_interval(non_active_customer_PCE_sen)
non_active_customer_PCE_sen_CI

non_active_customer_PCE_sen_df <- data.frame(
  Index = rep(1:1000, 2),
  Value = c(non_active_customer_PCE_sen, non_active_customer_PCE),
  Group = rep(c("Adjusted", "Unadjusted"), each = 1000)
)

ggplot(non_active_customer_PCE_sen_df, aes(x = Index, y = Value, color = Group)) +
  geom_point(size = 1) +
  geom_line(size = 0.5) +
  scale_color_manual(values = c("Adjusted" = "blue", "Unadjusted" = "orange")) +
  labs(
    title = "Posterior samples adjusted for bias due to violation of Assumption 3.3",
    x = "Index",
    y = "Value",
    color = "Vector"
  ) +
  theme_minimal()
```


